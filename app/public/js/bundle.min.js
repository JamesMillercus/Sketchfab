import { SketchfabModel } from './modules/SketchFab';
import { styles, test } from './modules/Functions';


let urlid = [],
	models = [];


$( ".url" ).each(function( index ) {
  urlid[index] = $( this ).text()
});


for(var x = 0;x<urlid.length;x++) models[x] = new SketchfabModel(urlid[x]);

styles(urlid.length);