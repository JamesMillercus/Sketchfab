extends ../layouts/sketchfab

block intro
	
	#singleContainer
		h1 User
		p: a.linkID(href = '/user') Back to main menu
		h1#title= data.audience.title
	#loadingScreen
	#sketchContainer
		p.url= data.audience.url
		p.description= data.audience.description

block content
	.container
		.row
			.col-md-21.col-lg-12
				article
					hr
					header
					// row
					// 	.col-md-6.col-lg-6
					// 		if data.audience.image
					// 			img(src=data.audience._.image.limit(680,680)).img-rounded

					// 	.col-md-6.col-lg-6
					// 		ul.list-group
					// 			li.list-group-item 
					// 				strong Price: £
					// 				strong= data.audience.price
					// 			li.list-group-item
					// 				strong Quantity: 
					// 				strong= data.audience.quantity
					// 			.well
					// 				strong Description: 
					// 				strong= data.audience.description
					// 			a.btn.btn-sucess.btn-block(href = "/contact") Ask About This Audience

block js
	script(src='/js/bundle.min.js')